# 📈 Big O Notation (O-нотация)

## 📑 Содержание
1. [Что это и зачем?](#1-что-это-и-зачем)
2. [Классы сложности](#2-классы-сложности)
3. [Space Complexity](#3-space-complexity)
4. [Шпаргалка](#4-шпаргалка)

---

## 1. 🤔 Что это и зачем?

**Big O** — это математическая нотация, описывающая, как меняется время выполнения алгоритма (или потребление памяти) при увеличении входных данных ($n$).

Мы не считаем секунды. Мы считаем **количество операций**.
Потому что секунды зависят от процессора, а количество операций — от алгоритма.

*   **Best Case** (Best scenario): Обычно не интересен.
*   **Average Case**: Важен.
*   **Worst Case** (Big O): Самый важный. "Хуже этого не будет".

---

## 2. 🐢 Классы сложности (от лучшего к худшему)

### O(1) — Constant Time
Время не зависит от $n$.
*   *Пример*: Взять элемент по индексу `arr[5]`.
*   *Пример*: Вставить в Map `m["key"] = val` (в среднем).

### O(log n) — Logarithmic Time
На каждом шаге отсекаем половину данных.
*   *Пример*: Binary Search (Бинарный поиск) в отсортированном массиве.
*   *Пример*: Поиск в сбалансированном дереве (AVL, Red-Black).

### O(n) — Linear Time
Нужно пройтись по всем элементам 1 раз.
*   *Пример*: Простой поиск (цикл `for`).
*   *Пример*: Сравнение двух строк.

### O(n log n) — Linearithmic Time
Стандарт для хороших сортировок.
*   *Пример*: Merge Sort, Quick Sort (в среднем), Heap Sort.
*   Суть: Мы делаем $n$ раз действие сложностью $\log n$.

### O(n²) — Quadratic Time
Вложенные циклы.
*   *Пример*: Bubble Sort (Пузырек).
*   *Пример*: Проверка всех пар элементов.

### O(2ⁿ) — Exponential Time
Каждый новый элемент удваивает время.
*   *Пример*: Рекурсивный расчет Фибоначчи (без кеша).
*   *Пример*: Перебор всех подмножеств.

### O(n!) — Factorial Time
Хуже некуда.
*   *Пример*: Задача коммивояжера (полный перебор маршрутов).

---

## 3. 💾 Space Complexity

То же самое, но про память.
Сколько **дополнительной** памяти нужно алгоритму?

*   **O(1)**: Сортировка пузырьком (меняем элементы местами in-place).
*   **O(n)**: Merge Sort (нужен массив такого же размера для слияния).

---

## 4. 📝 Шпаргалка

| Сложность | Название | При увеличении N в 10 раз, время... |
|---|---|---|
| **O(1)** | Constant | Не меняется |
| **O(log n)** | Logarithmic | Увеличивается на константу (немного) |
| **O(n)** | Linear | Увеличивается в 10 раз |
| **O(n log n)**| Linearithmic | Чуть больше чем в 10 раз |
| **O(n²)** | Quadratic | Увеличивается в 100 раз |
| **O(2ⁿ)** | Exponential | 💀 Взрывается (10 -> 1024) |
