# 📞 System Calls (Системные вызовы)

Системные вызовы — это единственный способ для программы из User Space попросить Ядро (Kernel Space) сделать что-то (записать на диск, отправить в сеть).

Примеры: `open`, `read`, `write`, `close`, `fork`, `exec`.
Утилита для отслеживания: `strace`.
