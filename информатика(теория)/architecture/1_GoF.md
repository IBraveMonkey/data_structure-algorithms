# 📚 GoF (Gang of Four) — Паттерны проектирования

## 📑 Содержание
1. [Что это?](#что-это)
2. [🏗️ Порождающие (Creational)](#порождающие-creational)
3. [🔌 Структурные (Structural)](#структурные-structural)
4. [🧠 Поведенческие (Behavioral)](#поведенческие-behavioral)

---

## 1. 🤔 Что это?

**Gang of Four (GoF)** — это четверо авторов легендарной книги "Design Patterns" (1994). Они описали 23 классических решения типичных проблем в ООП.

> [!NOTE]
> Паттерн — это не готовый код, а **идея** решения проблемы.

---

## 2. 🏗️ Порождающие (Creational)

Отвечают за то, как создаются объекты.

### 👑 Singleton (Одиночка)
Гарантирует, что у класса есть **только один** экземпляр, и предоставляет к нему глобальную точку доступа.
*   **Пример**: Подключение к базе данных, Логгер.

### 🏭 Factory Method (Фабричный метод)
Определяет интерфейс для создания объекта, но оставляет подклассам решение о том, какой класс инстанцировать.
*   **Пример**: "Служба доставки". Метод `createTransport()` вернет `Truck` (Грузовик) для дорожной логистики или `Ship` (Корабль) для морской.

### 👷 Builder (Строитель)
Позволяет создавать сложные объекты пошагово.
*   **Пример**: `HouseBuilder`. `buildWalls()`, `buildRoof()`, `buildPool()`. В итоге получаем Дом. Полезно, когда у конструктора 100500 параметров.

---

## 3. 🔌 Структурные (Structural)

Отвечают за то, как объекты компонуются в более крупные структуры.

### 🔌 Adapter (Адаптер)
Позволяет объектам с несовместимыми интерфейсами работать вместе.
*   **Пример**: Переходник с розетки США на Европу. Карточка памяти `SD` -> `USB`.

### 🎭 Decorator (Декоратор) / Обёртка
Динамически добавляет объекту новые обязанности.
*   **Пример**: "Пицца". Есть основа. Оборачиваем в "С сыром". Оборачиваем в "С грибами". Сам объект пиццы не меняется, но свойства добавляются.

### 🏛️ Facade (Фасад)
Предоставляет простой интерфейс к сложной системе классов.
*   **Пример**: Кнопка "Запустить машину". Вам не нужно знать порядок зажигания свечей, впрыска топлива и т.д. Вы просто жмете одну кнопку (Фасад), а она дергает сложную подсистему.

---

## 4. 🧠 Поведенческие (Behavioral)

Отвечают за взаимодействие и обязанности между объектами.

### 📡 Observer (Наблюдатель)
Создает механизм подписки, позволяющий одним объектам следить за событиями в других.
*   **Пример**: YouTube канал. Вы (Observer) подписались на канал (Subject). Когда выходит видео, вам приходит уведомление.

### 🗺️ Strategy (Стратегия)
Определяет семейство алгоритмов и делает их взаимозаменяемыми.
*   **Пример**: Навигатор. Стратегии: "На машине", "Пешком", "На автобусе". Задача одна (доехать из А в Б), но алгоритмы разные.

### 🗣️ Command (Команда)
Превращает запрос в объект. Это позволяет передавать запросы как аргументы, ставить их в очередь, логировать.
*   **Пример**: Кнопки на пульте ТВ. Каждая кнопка — это команда (`TurnOn`, `Mute`).