# 📚 GoF (Gang of Four) — Паттерны проектирования

## 📑 Содержание
1. [Что это?](#что-это)
2. [🏗️ Порождающие (Creational)](#порождающие-creational)
3. [🔌 Структурные (Structural)](#структурные-structural)
4. [🧠 Поведенческие (Behavioral)](#поведенческие-behavioral)
5. [🎯 Итог: Какой выбрать?](#итог-какой-выбрать)

---

## 1. 🤔 Что это?

**Gang of Four (GoF)** — это четверо авторов легендарной книги "Design Patterns" (1994). Они описали 23 классических решения типичных проблем в ООП.

> [!IMPORTANT]
> Паттерн — это не конкретный код, который можно скопировать, а **концепция** решения проблемы. Это как кулинарный рецепт: шаги одни и те же, но ингредиенты (язык программирования) могут отличаться.

---

## 2. 🏗️ Порождающие (Creational)

Эти паттерны отвечают за механизмы **создания объектов**. Они позволяют сделать систему независимой от способа создания, композиции и представления объектов.

### 👑 Singleton (Одиночка)

Гарантирует, что у класса есть **только один** экземпляр, и предоставляет к нему глобальную точку доступа.

*   **🛠️ Проблема**: Вам нужно, чтобы в системе существовал ровно один экземпляр чего-либо (например, конфиг, логгер или пул соединений с БД), и к нему можно было обратиться из любого места.
*   **💡 Решение**: Скрыть конструктор и создать статический метод, который всегда возвращает один и тот же объект.
*   **📦 Пример**: 
    ```go
    // В Go Singleton часто реализуется через sync.Once
    var instance *Database
    var once sync.Once

    func GetInstance() *Database {
        once.Do(func() {
            instance = &Database{} // Создастся только ПЕРВЫЙ раз
        })
        return instance
    }
    ```
*   **⚖️ Плюсы**: Контроль над общим ресурсом, экономия памяти.
*   **⚠️ Минусы**: Глобальное состояние (трудно тестировать), нарушение принципа единственной ответственности.

---

### 🏭 Factory Method (Фабричный метод)

Определяет интерфейс для создания объекта, но оставляет подклассам решение о том, какой класс инстанцировать.

*   **🛠️ Проблема**: Вы заранее не знаете, объекты каких типов вам понадобятся в будущем, или хотите дать возможность расширять библиотеку.
*   **💡 Решение**: Вместо вызова `new` напрямую, вы вызываете специальный "фабричный" метод.
*   **📦 Пример**: Приложение "Логистика". 
    - Базовая фабрика: `Logistic` с методом `CreateTransport()`.
    - Подкласс `RoadLogistic` создает `Truck`.
    - Подкласс `SeaLogistic` создает `Ship`.
*   **✅ Когда юзать**: Когда код должен работать с продуктами разных типов, но вы хотите скрыть детали их создания.

---

### 🏗️ Abstract Factory (Абстрактная фабрика)

Позволяет создавать **семейства** связанных объектов, не привязываясь к конкретным классам создаваемых объектов.

*   **🛠️ Проблема**: У вас есть наборы интерфейсов (например, "Мебель": Стул + Стол). Но эти наборы бывают разных стилей: "Викторианский", "Модерн". Вам нужно гарантировать, что все объекты в заказе будут в одном стиле.
*   **💡 Решение**: Создать абстрактную фабрику мебели, которая умеет создавать Стул и Стол. Конкретные реализации (ModernFactory, VictorianFactory) создают объекты своего стиля.
*   **📦 Пример**: UI-библиотека. Одна фабрика создает кнопки и чекбоксы для macOS, другая — для Windows.

---

### 👷 Builder (Строитель)

Разделяет конструирование сложного объекта от его представления, так что один и тот же процесс может создавать разные представления.

*   **🛠️ Проблема**: Огромный конструктор с 15 параметрами (многие из них необязательны) — так называемый "Anti-pattern: Telescoping Constructor".
*   **💡 Решение**: Вынести создание объекта в отдельный класс-строитель, который наполняет объект по частям.
*   **📦 Пример**: Сборка бургера.
    `NewBurgerBuilder().AddCheese().AddBacon().NoOnions().Build()`
*   **✅ Когда юзать**: Когда создание объекта включает много шагов или объект должен существовать в разных конфигурациях.

---

## 3. 🔌 Структурные (Structural)

Эти паттерны объясняют, как собирать объекты и классы в более крупные структуры, сохраняя их гибкость и эффективность.

### 🔌 Adapter (Адаптер)

Позволяет объектам с несовместимыми интерфейсами работать вместе.

*   **🛠️ Проблема**: У вас есть старый класс, который выдает данные в XML, и новый сервис, который понимает только JSON.
*   **💡 Решение**: Создать промежуточный объект (Адаптер), который оборачивает один интерфейс и делает его понятным для другого.
*   **📦 Пример**: Переходник `MicroUSB -> USB-C` или конвертер валют.

---

### 🎭 Decorator (Декоратор)

Динамически добавляет объекту новые обязанности, не меняя его исходный код.

*   **🛠️ Проблема**: Вам нужно расширить функционал класса, но наследоваться нельзя (или возникнет "комбинаторный взрыв" подклассов). Например, уведомления: в Telegram, в Email, в Telegram+Email.
*   **💡 Решение**: Поместить объект в "обертку", которая реализует тот же интерфейс и добавляет свое поведение до или после вызова основного метода.
*   **📦 Пример**: Система логирования. К базовому логгеру можно "навесить" декор `EncryptingLogger` (зашифрует лог) или `FileLogger` (запишет в файл).
*   **✅ Когда юзать**: Когда нельзя использовать наследование для расширения поведения.

---

### 🏛️ Facade (Фасад)

Предоставляет простой (высокоуровневый) интерфейс к сложной системе классов, библиотеке или фреймворку.

*   **🛠️ Проблема**: Ваша система состоит из 20 подсистем, и клиенту нужно дернуть 15 методов в разном порядке, чтобы просто загрузить видео.
*   **💡 Решение**: Создать "Фасад" — один класс с методом `UploadVideo()`, который сам внутри себя разбирается со всеми подсистемами.
*   **📦 Пример**: Кнопка "Заказать в 1 клик" в интернет-магазине. Она скрывает проверку остатков, оплату, логистику.

---

### 🛡️ Proxy (Заместитель)

Подменяет собой реальный объект, позволяя выполнять что-то до или после запроса к нему.

*   **🛠️ Проблема**: Реальный объект очень "тяжелый" (занимает много памяти) или требует контроля доступа (права).
*   **💡 Решение**: Клиент общается с Прокси. Прокси может: кешировать результаты, проверять права или загружать реальный объект только когда он реально понадобится (**Lazy Loading**).
*   **📦 Пример**: Кеширующий прокси для тяжелых запросов к БД.

---

### 🌲 Composite (Компоновщик)

Позволяет сгруппировать объекты в древовидную структуру и работать с ними так, будто это единый объект.

*   **🛠️ Проблема**: У вас есть "Коробка", в которой могут быть "Товары" или другие "Коробки". Вам нужно посчитать общую цену.
*   **💡 Решение**: И товар, и коробка реализуют один интерфейс `GetPrice()`. Коробка просто суммирует цены своего содержимого.
*   **📦 Пример**: Файловая система (Файлы и Папки). Папка — это композит, файл — лист.

---

## 4. 🧠 Поведенческие (Behavioral)

Эти паттерны заботятся об алгоритмах и **распределении обязанностей** между объектами.

### 📡 Observer (Наблюдатель)

Создает механизм подписки, позволяющий одним объектам следить за событиями в других.

*   **🛠️ Проблема**: У вас есть объект "Кнопка" и вы хотите, чтобы при нажатии на нее выполнялось 10 разных действий в разных частях программы, но вы не хотите "зашивать" эти действия в саму Кнопку.
*   **💡 Решение**: Объект (Subject) хранит список подписчиков. Когда происходит событие, он проходит по списку и вызывает метод у каждого подписчика.
*   **📦 Пример**: YouTube. Вы (Observer) подписались на канал (Subject). Когда выходит видео, всем подписчикам летит уведомление.
*   **✅ Когда юзать**: Когда изменение одного объекта требует изменения других, и вы не знаете заранее, сколько их.

---

### 🗺️ Strategy (Стратегия)

Определяет семейство похожих алгоритмов и позволяет переключать их прямо во время выполнения программы.

*   **🛠️ Проблема**: У вас есть огромный `switch` или куча `if-else` для выбора метода оплаты (Visa, PayPal, Crypto). Код становится нечитаемым.
*   **💡 Решение**: Вынести каждый алгоритм в отдельный класс. Основной класс просто хранит ссылку на текущую "стратегию" и вызывает её.
*   **📦 Пример**: Навигатор. Сегодня я еду на машине (одна стратегия), завтра иду пешком (другая). Задача одна — дойти, способы разные.

---

### 🗣️ Command (Команда)

Превращает запрос в отдельный объект. Это позволяет передавать запросы как параметры, ставить их в очередь или логировать.

*   **🛠️ Проблема**: Вы хотите реализовать кнопку "Отмена" (Undo) или очередь задач.
*   **💡 Решение**: Вместо вызова метода напрямую, вы создаете объект `Command`, который знает, какой метод вызвать. Команда может хранить состояние для отмены (Undo).
*   **📦 Пример**: Кнопка на пульте. Каждая кнопка — это объект "Команда" (`VolumeUp`, `Mute`). Официант в ресторане записывает заказ на листочек (это Команда) и несет его на кухню.

---

### 🧱 Template Method (Шаблонный метод)

Определяет скелет алгоритма, перекладывая ответственность за некоторые шаги на подклассы.

*   **🛠️ Проблема**: У вас есть два похожих процесса (например, парсинг CSV и JSON). Они оба: 1. Открывают файл, 2. Читают данные (по-разному), 3. Закрывают файл.
*   **💡 Решение**: В базовом классе создать метод, где шаги 1 и 3 реализованы, а шаг 2 — абстрактный (его допишут дети).
*   **📦 Пример**: Приготовление напитка: закипятить воду -> заварить основу (чай/кофе) -> налить в чашку -> добавить добавки (лимон/сахар). Скелет один, детали разные.

---

### 🚦 State (Состояние)

Позволяет объектам менять поведение в зависимости от их внутреннего состояния. Кажется, что объект сменил свой класс.

*   **🛠️ Проблема**: Огромный `switch` внутри каждого метода, который зависит от статуса (`if status == "Closed" { ... }`).
*   **💡 Решение**: Создать классы для каждого состояния (`OpenState`, `ClosedState`). Объект просто делегирует работу текущему состоянию.
*   **📦 Пример**: Банкомат. Его поведение (выдать деньги, выдать чек) полностью зависит от того, вставлена ли карта, введен ли ПИН и есть ли деньги.

---

### ⛓️ Chain of Responsibility (Цепочка обязанностей)

Позволяет передавать запросы последовательно по цепочке обработчиков.

*   **🛠️ Проблема**: Обработка запроса состоит из нескольких этапов (авторизация, логирование, валидация). Если один этап не прошел, дальше не идем.
*   **💡 Решение**: Каждый обработчик содержит ссылку на следующий. Если он не может обработать запрос или должен передать его дальше — он дергает следующего.
*   **📦 Пример**: Служба поддержки. Сначала говорит робот -> потом младший спец -> потом старший менеджер. Если робот решил проблему — цепочка прерывается.

---

## 🎯 Итог: Какой выбрать?

1.  Нужно создать сложный объект? — **Creational** (Builder, Factory).
2.  Нужно соединить несовместимое или упростить структуру? — **Structural** (Adapter, Facade).
3.  Нужно наладить связь между объектами или сменить алгоритм на лету? — **Behavioral** (Observer, Strategy).