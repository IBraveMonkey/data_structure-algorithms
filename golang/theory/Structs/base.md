### 🏗️ Структуры и методы в Go

Структура (`struct`) — это композитный тип данных, который позволяет объединить поля различных типов в одну сущность. В Go нет классов, поэтому структуры являются основным способом моделирования данных и поведения.

---

# 1. 🧩 Основы структур

Структура — это набор именованных полей.

```go
type User struct {
    ID    int
    Name  string
    Email string
}
```

### ⚙️ Ключевые свойства
- **Сравнение**: Структуры можно сравнивать (`==`, `!=`), если все их поля являются **сравнимыми** (например, числа, строки, указатели). Если структура содержит срез (`slice`) или карту (`map`), она несравнима.
- **Нулевое значение**: Если поля не инициализированы, они получают свои нулевые значения (`0`, `""`, `nil`).
- **Литералы**: Можно инициализировать по именам полей или по порядку (не рекомендуется для больших структур).

---

# 2. 🛠️ Методы: Синтаксический сахар

Метод — это функция с особым аргументом — **receiver** (получателем).

```go
func (u User) SayHello() {
    fmt.Printf("Привет, я %s\n", u.Name)
}
```

### 🍭 Под капотом
Методы в Go — это просто **синтаксический сахар**. Вызов `u.SayHello()` компилятор превращает в обычный вызов функции, где объект передается первым аргументом:

```go
// Логически это превращается в:
User.SayHello(u)
```

Это объясняет, почему методы можно вызывать даже для типов, которые не являются структурами (например, для своих типов на базе `int`).

---

# 3. 🎯 Pointer vs Value Receivers

Это самый частый вопрос на собеседованиях.

| Receiver Type | Копирование? | Изменяет оригинал? | Когда использовать? |
| :--- | :--- | :--- | :--- |
| **Value** `(u User)` | Да (копия объекта) | Нет | Маленькие структуры, неизменяемые данные. |
| **Pointer** `(u *User)` | Нет (копия адреса) | **Да** | Нужно изменить состояние или структура большая (чтобы не копировать). |

> [!IMPORTANT]
> Если хотя бы один метод типа имеет **Pointer Receiver**, рекомендуется делать все методы этого типа такими же для консистентности.

---

# 4. 🧬 Nil Receivers

Так как методы — это функции, принимающие объект первым аргументом, мы можем вызывать метод даже на `nil` указателе, если внутри метода есть проверка.

```go
func (u *User) GetName() string {
    if u == nil {
        return "Guest"
    }
    return u.Name
}

var u *User = nil
fmt.Println(u.GetName()) // Выведет "Guest", а не вызовет панику!
```

> [!CAUTION]
> Если метод попытается обратиться к полю `u.Name` при `u == nil` без проверки, возникнет **panic: runtime error: invalid memory address or nil pointer dereference**.

---

# 5. 🏗️ Embedding (Внедрение) и Shadowing

В Go нет наследования. Вместо него используется **композиция** через внедрение анонимных полей.

```go
type Admin struct {
    User  // Внедрение (embedding)
    Level int
}

a := Admin{User: User{Name: "Иван"}, Level: 10}
fmt.Println(a.Name) // Прямой доступ к полю вложенной структуры
```

### 🌑 Shadowing (Затенение)
Если в `Admin` есть поле `Name`, оно "затенит" поле `Name` из `User`. Чтобы получить доступ к затененному полю, нужно обратиться к нему через тип: `a.User.Name`.

---

# 6. ⚔️ Наследование vs Композиция

Go пропагандирует композицию.

- **Наследование (классическое)**: Объект "является" (is-a) родителем. Строгая иерархия.
- **Композиция (Go)**: Объект "содержит" (has-a) или "пользуется" поведением. Гибкость и отсутствие жестких зависимостей.

---

# 7. 🏷️ Struct Tags (Теги)

Теги — это метаданные, используемые рефлексией (например, для JSON или работы с БД).

```go
type User struct {
    Name string `json:"user_name" db:"name"`
    Age  int    `json:"age,omitempty"` // omitempty — скрыть, если 0
}
```

---

# 📊 Сводка: Выбор Receiver

1. **Меняете состояние?** -> Pointer.
2. **Структура большая (> 64 байт)?** -> Pointer.
3. **Нужна безопасность от изменений?** -> Value.
4. **Это `map`, `chan` или `func`?** -> Value (они и так ссылочные).
5. **Это `slice`?** -> Pointer, если метод меняет длину (len/cap).
