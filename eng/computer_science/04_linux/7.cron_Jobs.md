# ⏰ Cron Jobs (Scheduler)

Cron allows you to schedule tasks to run automatically at specific times or intervals.

**Syntax**: `min hour day_of_month month day_of_week`

**Example**: `0 3 * * * /path/to/script.sh` (Runs every day at 3:00 AM).

**Control Command**: `crontab -e` (Edits the current user's crontab file).

<!-- QUIZ_START 
[
    {
        "question": "What does the Cron syntax look like for running a task at 3:00 AM every day?",
        "options": ["* 3 * * *", "3 0 * * *", "0 3 * * *", "0 0 3 * *"],
        "correctIndex": 2
    },
    {
        "question": "Which command is used to edit the Cron task list for the current user?",
        "options": ["cron-edit", "crontab -e", "crontab -l", "sudo cron"],
        "correctIndex": 1
    },
    {
        "question": "Which Cron value corresponds to running a task every minute?",
        "options": ["60 * * * *", "1 * * * *", "* * * * *", "0 * * * *"],
        "correctIndex": 2
    }
]
QUIZ_END -->
